# Electricity Demand & Grid Stress Analysis
Analyze hourly electricity demand, renewable generation and stress periods using rolling baselines, lag features and anomaly flags.

Place hourly data at `data/grid.csv`, then run `python main.py`.

Questions: When are demand peaks? When does renewable supply lag demand? Which hours are unusually stressed? See `analysis_notes.txt` for full logic.