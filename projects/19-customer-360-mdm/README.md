# Customer 360 Master Data Management
Integrate fragmented CRM, sales and marketing HCP records into a governed Customer 360 using standardization, deterministic matching, fuzzy entity resolution and golden records.

Run `python main.py` after placing `crm.csv`, `sales.csv`, and `marketing.csv` in `data/`.

This is the most data-engineering-oriented project in the set.